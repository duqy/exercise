#include <iostream>

using namespace std;

// emacs yasnippet≤‚ ‘

int main(int argc, char *argv[])
{
  vector<int> a {2, 3, 4, 5};
  for (int i = 0; int(a.size()); ++i) {
    cout << a[i] << "\n";
  }

  if (a.size()>0) {
    while (true) {
      ;;
    }
    switch (expr) {
    case constexpr: {
      
      break;
    }
    default:
      break;
    }
  }
  return 0;
}


